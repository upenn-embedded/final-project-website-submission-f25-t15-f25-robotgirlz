//define the functions that will acctually read the i2c data and make it a number this shoyld be a SRS buuuut 

#include "TEMPSENSOR/tempi2c.h"
#include "lib/i2cdriver.h"

/* Six i2c_reads are needed to obtain necessary raw object values from the sensor. These values are grouped and then
 * averaged before return of the function. After that they are then preprocessed before going to
 * calculation functions*/
tempaddr = 0x28 / DOUBLE CHEKC SENSOR ADDR

int degtemp(){
    addr = tempaddr 
     I2C0_read_bytes(uint8_t addr, uint8_t *data, uint8_t 6)
    
    
    //get temp adn convert to deg
}